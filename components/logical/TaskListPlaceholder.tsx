// React
import React from 'react';

// Components
import ListPlaceholder from '../reusables/ListPlaceholder';

const TaskListPlaceholder = React.memo(() => (
  <ListPlaceholder>
    Drop here.
  </ListPlaceholder>
));

export default TaskListPlaceholder;
